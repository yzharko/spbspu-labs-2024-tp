#ifndef COMMANDS_HPP
#define COMMANDS_HPP

#include <string>

namespace ponomarev
{
  void outputInfoAboutCommands(const std::string & parameters);
  void makeInput(std::string & parameters);
}

#endif
